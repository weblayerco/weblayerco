# Optional: URL of a backend that runs a real pronunciation model.
# When set, the app uses ServerAssessor instead of the local heuristic.
# The endpoint receives multipart form-data (audio, letter, harakah)
# and must return JSON: { "pass": bool, "score": number, "heard": string, "message": string }
VITE_ASSESS_URL=
